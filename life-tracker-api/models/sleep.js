const db = require("../db")

class sleepPost {
    static async listSleepPost() {
        //list all post in db in descending oerder of when they were created
    }

    static async createSleepPost() {
        //create new sleep post
    }
}

module.exports = sleepPost
